version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./generated_reports:/app/generated_reports
    environment:
      - PYTHONUNBUFFERED=1
